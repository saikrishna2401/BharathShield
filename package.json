{
  "name": "bharathshield-root",
  "version": "2.0.0",
  "description": "BharathShield - Multilingual Phishing SMS Detection & Alert System",
  "scripts": {
    "start": "npx concurrently --kill-others \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev": "npx concurrently --kill-others \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start:prod": "node backend/server.js",
    "test": "npm test --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
